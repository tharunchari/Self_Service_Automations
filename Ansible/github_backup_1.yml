---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: print starting date and time
      shell: date
      register: date_1

    - debug:
        msg: "{{ date_1.stdout }}"
