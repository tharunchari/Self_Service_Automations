
---
- hosts: all
  tasks:
  
    - name: Start VM-BAMBOOPREPRD-TEST-AGENT.vitech.com
      shell: |
       source /home/bamboo/.bash_profile
       /u01/BambooCLOUDAGNT/bin/bamboo-agent.sh start
      become: true 
      become_user: bamboo  
      when: ansible_host == '172.26.46.219'  # only run on specified host
      
   
