name: "dataretention"
on:
  workflow_dispatch:

jobs:
  check_instances:
    runs-on: ubuntu-latest
 
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Open Web Client Directory
        run: |
          ls -la
          pwd
      - name: Navigate to folder
        run: cd PYTHON_Script
      - name: Open Web Client Directory
        run: |
          ls -la
          pwd

      - name: Create AWS CodeCommit Repository
        run: |         
         aws codecommit create-repository --repository-name skk --region us-west-2
      #- name: Configure AWS Credentials
      #  uses: aws-actions/configure-aws-credentials@v1
      #  with:
       #   aws-access-key-id: ${{ V3LOCITY_CODECOMMIT_USERNAME }}
       #   aws-secret-access-key: ${{ V3LOCITY_CODECOMMIT_PASSWORD }}
       #   aws-region: us-west-2
 
