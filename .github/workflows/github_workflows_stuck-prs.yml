name: Stuck PRs Email Report (Always Use gh pr checks)

on:
  workflow_dispatch:

jobs:
  check-schedule:
    runs-on: ubuntu-latest
    outputs:
      should_proceed: ${{ steps.check.outputs.should_proceed }}
    steps:
      - id: check
        run: echo "should_proceed=true" >> $GITHUB_OUTPUT

  start-runner:
    needs: check-schedule
    if: needs.check-schedule.outputs.should_proceed == 'true'
    uses: vitechsystems/Internal-Tools_sub_workflows/.github/workflows/start_runner.yml@main
    with:
      runner_name: "Internal_Tools_Automation_Server"
    secrets: inherit

  execute-job:
    needs: [check-schedule, start-runner]
    if: needs.check-schedule.outputs.should_proceed == 'true'
    runs-on: [self-hosted, Internal_Tools_Automation_Server]
    env:
      ORG_NAME: vitechsystems
      SES_FROM: v3atlassianops@vitechinc.com
      SES_TO: v3atlassianops@vitechinc.com
      SES_SUBJECT: 'Vitechsystems Org PRs Detected With Stuck Status Checks'
    steps:
      - name: Install jq and gh
        run: |
          set -euo pipefail
          if ! command -v jq > /dev/null 2>&1; then
            sudo apt-get update && sudo apt-get install -y jq || sudo yum install -y jq
          fi

          if ! command -v gh > /dev/null 2>&1; then
            ARCH=$(uname -m)
            GH_VERSION="2.48.0"
            GH_ARCH="amd64"; [ "$ARCH" = "aarch64" ] && GH_ARCH="arm64"
            if command -v yum > /dev/null 2>&1; then
              curl -LO https://github.com/cli/cli/releases/download/v$GH_VERSION/gh_${GH_VERSION}_linux_${GH_ARCH}.rpm
              sudo yum localinstall -y gh_${GH_VERSION}_linux_${GH_ARCH}.rpm
              rm -f gh_${GH_VERSION}_linux_${GH_ARCH}.rpm
            else
              curl -LO https://github.com/cli/cli/releases/download/v$GH_VERSION/gh_${GH_VERSION}_linux_${GH_ARCH}.deb
              sudo dpkg -i gh_${GH_VERSION}_linux_${GH_ARCH}.deb
              rm -f gh_${GH_VERSION}_linux_${GH_ARCH}.deb
            fi
          fi

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.CORP_CLD_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.CORP_CLD_AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Get all repos in org
        env:
          GH_TOKEN: ${{ secrets.PROD_FINE_GRAINED_PAT }}
        run: |
          set -euo pipefail
          export GH_TOKEN="${GH_TOKEN}"
          gh repo list "$ORG_NAME" --limit 1000 --json name -q '.[] | .name' > repos.txt

      - name: Check PRs and build report
        env:
          GH_TOKEN: ${{ secrets.PROD_FINE_GRAINED_PAT }}
        run: |
          set -euo pipefail
          trap 'echo "ERROR: Job failed at line $LINENO"; exit 1' ERR

          export GH_TOKEN="${GH_TOKEN}"
          NOW=$(date -u +%s)
          DELIM=$'\x1e'
          declare -a STUCK_PRS
          AGE_LIMIT=$((60 * 60)) # 1 hour

          while read repo; do
            echo "==== Checking repository: $repo ===="
            prs=$(gh pr list --repo "$ORG_NAME/$repo" --state open --json number,createdAt,title 2>&1)
            if ! jq empty <<< "$prs" > /dev/null 2>&1; then
              echo "::warning:: Failed to parse PR list for $repo"
              continue
            fi

            pr_count=$(jq 'length' <<< "$prs")
            for i in $(seq 0 $((pr_count - 1))); do
              pr=$(jq ".[$i]" <<< "$prs")
              number=$(jq -r '.number' <<< "$pr")
              title=$(jq -r '.title' <<< "$pr")
              created=$(jq -r '.createdAt' <<< "$pr")
              created_ts=$(date -d "$created" +%s || echo 0)
              age_secs=$(( NOW - created_ts ))
              pr_url="https://github.com/$ORG_NAME/$repo/pull/$number"

              if [ $age_secs -lt $AGE_LIMIT ]; then
                continue
              fi

              # Get latest commit SHA
              pr_commits=$(gh pr view "$number" --repo "$ORG_NAME/$repo" --json commits 2>&1)
              commit_sha=$(jq -r '.commits[-1].oid // empty' <<< "$pr_commits" || echo "")
              if [ -z "$commit_sha" ]; then
                echo "::warning:: No commit SHA found for PR #$number in $repo"
                continue
              fi

              # Get check runs
              echo "Fetching check runs for $repo PR #$number (commit: $commit_sha)"
              if ! checks_json=$(gh api repos/$ORG_NAME/$repo/commits/$commit_sha/check-runs -H "Accept: application/vnd.github.v3+json" 2>&1); then
                echo "::warning:: Failed to fetch check runs for $repo PR #$number"
                echo "gh api error: $checks_json"
                continue
              fi
              
              if ! jq empty <<< "$checks_json" > /dev/null 2>&1; then
                echo "::warning:: Invalid JSON received when fetching check runs for $repo PR #$number"
                echo "Raw response: $checks_json"
                continue
              fi

              pending_total=$(jq '[.check_runs[] | select(.status == "queued" or .status == "in_progress")] | length' <<< "$checks_json")
              pending_names=$(jq -r '[.check_runs[] | select(.status == "queued" or .status == "in_progress") | .name] | join(", ")' <<< "$checks_json")

              if [ "$pending_total" -eq 0 ]; then
                continue
              fi

              # PR metadata
              pr_json=$(gh pr view "$number" --repo "$ORG_NAME/$repo" --json author,reviewRequests,state 2>&1)
              author=$(jq -r '.author.login // "unknown"' <<< "$pr_json" || echo "unknown")
              state=$(jq -r '.state // "unknown"' <<< "$pr_json" || echo "unknown")
              reviewers=$(jq -r '
                if has("reviewRequests") then
                  if (.reviewRequests | type == "object" and .reviewRequests.nodes) then
                    ([.reviewRequests.nodes[].requestedReviewer.login // .reviewRequests.nodes[].requestedReviewer.name // ""] | map(select(length > 0)) | join(", "))
                  elif (.reviewRequests | type == "array") then
                    ([.reviewRequests[].requestedReviewer.login // .reviewRequests[].requestedReviewer.name // ""] | map(select(length > 0)) | join(", "))
                  else "" end
                else "" end
              ' <<< "$pr_json" || echo "")

              STUCK_PRS+=( "${repo}${DELIM}${number}${DELIM}${pr_url}${DELIM}${title}${DELIM}${author}${DELIM}${created}${DELIM}${pending_names}${DELIM}${state}${DELIM}${reviewers}" )
            done
          done < repos.txt

          {
            echo "<html><head><style>"
            echo "body { font-family: sans-serif; background: #f4f6fb; }"
            echo "table { border-collapse: collapse; width: 100%; }"
            echo "th, td { border: 1px solid #ccc; padding: 10px; }"
            echo "th { background: #3f51b5; color: white; }"
            echo "</style></head><body>"
            echo "<p>The following PRs are open >1 hour with pending checks:</p>"
            echo "<table><tr><th>Repo</th><th>PR</th><th>Title</th><th>Author</th><th>Created</th><th>Pending Checks</th><th>Status</th><th>Reviewers</th></tr>"
            for pr in "${STUCK_PRS[@]}"; do
              IFS="$DELIM" read -r repo number pr_url title author created pending_names state reviewers <<< "$pr"
              echo "<tr><td>$repo</td><td><a href=\"$pr_url\">#$number</a></td><td>$title</td><td>$author</td><td>$created</td><td>$pending_names</td><td>$state</td><td>$reviewers</td></tr>"
            done
            echo "</table></body></html>"
          } > email_body.html

          if [ ${#STUCK_PRS[@]} -gt 0 ]; then
            aws ses send-email \
              --from "$SES_FROM" \
              --destination "ToAddresses=$SES_TO" \
              --message "Subject={Data='$SES_SUBJECT'},Body={Html={Data=\"$(cat email_body.html)\"}}"
            echo "Email sent via SES for stuck PRs"
          else
            echo "No stuck PRs found. Email not sent."
          fi

  stop-runner:
    needs: [execute-job]
    if: always()
    uses: vitechsystems/Internal-Tools_sub_workflows/.github/workflows/stop_runner.yml@main
    with:
      runner_name: "Internal_Tools_Automation_Server"
    secrets: inherit
